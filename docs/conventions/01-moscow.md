# MoSCoW Priority

## Rules

- `Must`: 반드시 있어야 하는 것
- `Should`: 있으면 매우 좋은 것
- `Could`: 있으면 좋지만 없어도 되는 것
- `Won't`: 이번 범위에서는 하지 않는 것

## Usage

- 새 기능을 넣을 때 먼저 분류한다.
- 문서, 코드, 데모 범위를 정할 때도 같은 기준을 쓴다.
- `Must`가 아니면 이번 작업 범위에 넣지 않는 것을 기본으로 한다.

