# Branch Conventions

브랜치에 대한 기준 문서다.
실행 체크리스트는 `docs/ops/workflows/working-tree.md`를 참고한다.

## Rules

- 기본 브랜치에서 바로 작업하지 않는다.
- 목적이 드러나는 브랜치에서 작업한다.
- 브랜치명은 영어 소문자와 하이픈을 쓴다.
- 한 브랜치에는 하나의 작업만 담는다.

## Suggested Prefix

- `feat/`
- `fix/`
- `docs/`
- `refactor/`
- `hotfix/`

